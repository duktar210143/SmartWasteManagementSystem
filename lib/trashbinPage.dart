import 'package:flutter/material.dart';

class trashbinPage extends StatelessWidget {
  const trashbinPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("This is the trash can page"),
      ),
    );
  }
}
